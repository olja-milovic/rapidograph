export enum Attribute {
  Orientation = "orientation",
  XAxisPosition = "x-axis-position",
  YAxisPosition = "y-axis-position",
  Theme = "theme",
  TooltipTheme = "tooltip-theme",
  ShowLabels = "show-labels",
}

export enum Orientation {
  Horizontal = "horizontal",
  Vertical = "vertical",
}

export enum XAxisPosition {
  Top = "top",
  Bottom = "bottom",
}

export enum YAxisPosition {
  Left = "left",
  Right = "right",
}

export enum Theme {
  Light = "light",
  Dark = "dark",
}

export enum ShowLabels {
  Always = "always",
  Never = "never",
  Hover = "hover",
}

export enum TooltipPosition {
  Left = "left",
  Right = "right",
  Top = "top",
  Bottom = "bottom",
}
